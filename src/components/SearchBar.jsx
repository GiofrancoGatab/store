import react from "react";

const SearchBar = () => {
  return (
    <div>
      <input type="text">
        <div>
          <input type="CheckBox">Only Show products in stocks</input>
        </div>
      </input>
    </div>
  );
};

export default SearchBar;
